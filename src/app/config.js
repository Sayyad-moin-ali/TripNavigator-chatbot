import { createChatBotMessage } from "react-chatbot-kit";

const config = {
    botName: "TripNavigator",
    initialMessages: [
        createChatBotMessage("Hello i am TripNavigator. how can i help you"),
    ]
}
export default config;